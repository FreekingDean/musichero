<%= form_with(model: spotify_platlist, class: "contents") do |form| %>
  <% if spotify_platlist.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(spotify_platlist.errors.count, "error") %> prohibited this spotify_platlist from being saved:</h2>

      <ul class="list-disc ml-6">
        <% spotify_platlist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :url %>
    <%= form.text_field :url, class: ["block shadow rounded-md border outline-none px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": spotify_platlist.errors[:url].none?, "border-red-400 focus:outline-red-600": spotify_platlist.errors[:url].any?}] %>
  </div>

  <div class="inline">
    <%= form.submit class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
